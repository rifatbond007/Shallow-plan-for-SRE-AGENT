# SRE AI Agent - Environment Variables
# Copy this file to .env and fill in your values.
#
# All vars are prefixed SRE_AGENT_ per SPECIFICATION.md §4.1.
# Only the Claude API is an outbound dependency (spec §1.2).
# No Redis, no Postgres, no S3 in v1.

# =============================================================================
# HTTP server
# =============================================================================
SRE_AGENT_PORT=8080
SRE_AGENT_LOG_LEVEL=info

# =============================================================================
# Anthropic (Claude) API  — required
# =============================================================================
# Get your key from https://console.anthropic.com/
SRE_AGENT_ANTHROPIC_API_KEY=sk-ant-your-api-key-here
SRE_AGENT_ANTHROPIC_MODEL=claude-sonnet-4-20250514
SRE_AGENT_ANTHROPIC_MAX_TOKENS=2048
SRE_AGENT_ANTHROPIC_TIMEOUT=30s

# =============================================================================
# Codebase analysis
# =============================================================================
SRE_AGENT_CODEBASE_PATH=/codebase
SRE_AGENT_CODEBASE_CACHE_DIR=/tmp/sre-agent/cache

# =============================================================================
# Ingestion
# =============================================================================
SRE_AGENT_MAX_LOG_BYTES=5000000
SRE_AGENT_INCIDENT_WINDOW=5m
SRE_AGENT_INCIDENT_MIN_SIZE=3

# =============================================================================
# Analysis
# =============================================================================
SRE_AGENT_HYPOTHESIS_TOP_K=3

# =============================================================================
# Cache
# =============================================================================
SRE_AGENT_CACHE_TTL=1h
SRE_AGENT_CACHE_MAX_ENTRIES=512

# =============================================================================
# Rate limiting (in-process token bucket, per IP)
# =============================================================================
SRE_AGENT_RATE_LIMIT_RPS=5
SRE_AGENT_RATE_LIMIT_BURST=10

# =============================================================================
# API auth (optional, spec §4.1 / DEVOPS §5.1)
# If unset, /api/v1/analyze is open and a warning is logged at startup.
# =============================================================================
# SRE_AGENT_API_KEY=replace-me-with-a-long-random-string
